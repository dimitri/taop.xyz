+++
title = "Querying Data"
weight = 2
icon = "fa-solid fa-magnifying-glass"
summary = "The SQL toolbox: SELECT, joins, grouping, NULLs, window functions, and the patterns that replace application code."
+++

This is the heart of The Art of PostgreSQL — the toolbox. From `SELECT … FROM …
WHERE` to window functions and nested `LATERAL` joins, these lessons show how to
express real business logic as a single, readable query.
